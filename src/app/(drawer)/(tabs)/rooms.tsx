import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export default function RoomsScreen () {
    return (
        <SafeAreaView >
            <Text>
                this is rooms screen
            </Text>
        </SafeAreaView>
    )
}